{
  "name": "novelchain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "env": "node scripts/generate-env.mjs",
    "start": "npm run env && node scripts/serve.mjs",
    "check": "node --check app.js && node --check scripts/generate-env.mjs && node --check scripts/serve.mjs"
  }
}
